node_modules
.next
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Atlas credentials handed over by the operator — never commit
atlas-credentials.env

# Optimization tooling, not part of the app
/optimize-web
